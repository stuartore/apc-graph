# apc-graph

Usage:
```
git clone https://github.com/stuartore/apc-graph.git
cd apc-graph && sudo chmod +x gen.sh

# Example
./gen /{some_place}/audio_policy_configuration.xml {graph_name}
./gen audio_policy_configuration.xml example

```
